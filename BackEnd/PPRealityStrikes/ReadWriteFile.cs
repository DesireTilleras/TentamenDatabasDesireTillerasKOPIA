using System;
using System.Collections.Generic;
using System.IO;
using System.Text;

namespace HemtentaDesireDatabaserPragueParking
{
    class ReadWriteFile
    {
        // Metoder i de olika klasserna kallar på denna funktionen. 
        // Metoden tar in sträng om vilken fil som skall skrivas till. 
        // Skriver till olika filer från klasserna CustomerInteraction, InvoiceProcessing

        // CheckOut(){ write(namn på filen)}

        // DesignDocument() (to string()??)

        public File Write(string info, string fileName)
        {
            //StreamWriter sw = new StreamWriter(filename)

            //return sw.WriteLine(Info);
        }
    }
}
